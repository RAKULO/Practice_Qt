#ifndef AREA_H
#define AREA_H
//setting marco with header file
#define PI 3.14
#define CIRCLE(r) ((PI)*(r)*(r))
#define RECTANGLE(length, height) ((length)*(height))
#define TRIANGLE(base, height) ((base)*(height)/2)

class area
{
public:
    //creat area() class
    area();
};

#endif // AREA_H
